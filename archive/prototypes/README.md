# Archived Prototype Demos

This folder keeps earlier standalone Lighthouse prototype demos that are useful as product references but are not the active Review Room service.

Active Review Room work lives in `services/review-room-service/` and `docs/plans/`.
