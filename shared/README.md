# Shared

This directory is reserved for code or docs that are genuinely reused by more than one experiment.

Keep new logic inside its experiment first. Promote it here only after a second experiment needs the same helper, schema, fixture, or workflow.
